<UserControl x:Class="SalesManagement.Desktop.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>   <!-- Product Grid (65%) -->
            <ColumnDefinition Width="380"/> <!-- Add/Edit Form (35%) -->
        </Grid.ColumnDefinitions>

        <!-- Product Table & Search -->
        <Grid Grid.Column="0" Margin="0,0,16,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search Header -->
            <Border Grid.Row="0" Style="{StaticResource CardPanel}" Margin="0,0,0,12" Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="🔍 البحث عن منتج:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Height="38"/>
                    <Button Grid.Column="2" Content="🔄 تحديث" Command="{Binding LoadProductsCommand}" Style="{StaticResource PrimaryButton}" Margin="10,0,0,0"/>
                </Grid>
            </Border>

            <!-- Products DataGrid -->
            <Border Grid.Row="1" Style="{StaticResource CardPanel}" Padding="0">
                <DataGrid ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="الباركود" Binding="{Binding Barcode}" Width="130"/>
                        <DataGridTextColumn Header="اسم المنتج" Binding="{Binding NameAr}" Width="*"/>
                        <DataGridTextColumn Header="التصنيف" Binding="{Binding CategoryName}" Width="110"/>
                        <DataGridTextColumn Header="سعر الشراء" Binding="{Binding PurchasePrice, StringFormat='{}{0:N2} دج'}" Width="110"/>
                        <DataGridTextColumn Header="سعر البيع" Binding="{Binding SalePrice, StringFormat='{}{0:N2} دج'}" Width="110"/>
                        <DataGridTextColumn Header="المخزون المتوفر" Binding="{Binding CurrentStock, StringFormat='{}{0:N0}'}" Width="110"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Add / Edit Form Panel -->
        <Border Grid.Column="1" Style="{StaticResource CardPanel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="بيانات المنتج" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>

                    <TextBlock Text="الباركود:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding Barcode, UpdateSourceTrigger=PropertyChanged}" Height="38"/>
                        <Button Grid.Column="1" Content="توليد باركود" Command="{Binding GenerateBarcodeCommand}" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Padding="8,0"/>
                    </Grid>

                    <TextBlock Text="اسم المنتج (بالعربية):" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding NameAr, UpdateSourceTrigger=PropertyChanged}" Height="38" Margin="0,0,0,12"/>

                    <TextBlock Text="اسم المنتج (بالفرنسية):" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding NameFr, UpdateSourceTrigger=PropertyChanged}" Height="38" Margin="0,0,0,12"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,6,0">
                            <TextBlock Text="سعر الشراء (دج):" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding PurchasePrice, UpdateSourceTrigger=PropertyChanged}" Height="38"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="6,0,0,0">
                            <TextBlock Text="سعر البيع للتجزئة:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding SalePrice, UpdateSourceTrigger=PropertyChanged}" Height="38"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,6,0">
                            <TextBlock Text="سعر الجملة (دج):" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding WholesalePrice, UpdateSourceTrigger=PropertyChanged}" Height="38"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="6,0,0,0">
                            <TextBlock Text="حد تنبيه النقص:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding MinStockAlert, UpdateSourceTrigger=PropertyChanged}" Height="38"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="الرصيد الأولي للمخزون:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding InitialStock, UpdateSourceTrigger=PropertyChanged}" Height="38" Margin="0,0,20,0"/>

                    <Button Content="💾 حفظ المنتج" Command="{Binding SaveProductCommand}" Style="{StaticResource SuccessButton}" Height="45" Margin="0,0,0,8"/>
                    <Button Content="➕ منتج جديد" Command="{Binding NewProductCommand}" Style="{StaticResource PrimaryButton}" Height="40"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
