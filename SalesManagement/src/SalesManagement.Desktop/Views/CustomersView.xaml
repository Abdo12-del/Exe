<UserControl x:Class="SalesManagement.Desktop.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>   <!-- Customers List (50%) -->
            <ColumnDefinition Width="*"/>   <!-- Statement & Payment (50%) -->
        </Grid.ColumnDefinitions>

        <!-- Left: Customer Directory -->
        <Grid Grid.Column="0" Margin="0,0,16,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search & Filter -->
            <Border Grid.Row="0" Style="{StaticResource CardPanel}" Margin="0,0,0,12" Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" ToolTip="ابحث بالاسم أو الهاتف..."/>
                    <CheckBox Grid.Column="1" Content="أصحاب الديون فقط" IsChecked="{Binding FilterDebtOnly}" VerticalAlignment="Center" Margin="12,0,0,0"/>
                </Grid>
            </Border>

            <!-- Customers Table -->
            <Border Grid.Row="1" Style="{StaticResource CardPanel}" Padding="0">
                <DataGrid ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="الهاتف" Binding="{Binding Phone}" Width="110"/>
                        <DataGridTextColumn Header="الرصيد المدين (الكريدي)" Binding="{Binding CurrentDebt, StringFormat='{}{0:N2} دج'}" Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Right: Statement & Debt Settlement -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Debt Payment Box -->
                <RowDefinition Height="*"/>    <!-- Account Statement Grid -->
            </Grid.RowDefinitions>

            <!-- Debt Repayment Panel -->
            <Border Grid.Row="0" Style="{StaticResource CardPanel}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="تسديد ديون الزبون (Règlement)" FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding PaymentAmount, UpdateSourceTrigger=PropertyChanged}" Height="38" FontSize="16" FontWeight="Bold" Margin="0,0,8,0"/>
                        <TextBox Grid.Column="1" Text="{Binding PaymentNotes, UpdateSourceTrigger=PropertyChanged}" Height="38" ToolTip="ملاحظات التسديد..."/>
                        <Button Grid.Column="2" Content="💵 تسجيل الدفعة" Command="{Binding RecordPaymentCommand}" Style="{StaticResource SuccessButton}" Height="38" Margin="8,0,0,0"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Statement Table -->
            <Border Grid.Row="1" Style="{StaticResource CardPanel}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#F8FAFC" Padding="12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                        <TextBlock Text="كشف الحساب التفصيلي (Relevé de Compte)" FontWeight="Bold"/>
                    </Border>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding StatementItems}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat='{}{0:yyyy-MM-dd}'}" Width="100"/>
                            <DataGridTextColumn Header="البيان" Binding="{Binding Description}" Width="*"/>
                            <DataGridTextColumn Header="مدين (+)" Binding="{Binding Debit, StringFormat='{}{0:N2}'}" Width="100"/>
                            <DataGridTextColumn Header="دائن (-)" Binding="{Binding Credit, StringFormat='{}{0:N2}'}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
