<UserControl x:Class="SalesManagement.Desktop.Views.ExpensesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>   <!-- Expenses Ledger (65%) -->
            <ColumnDefinition Width="380"/> <!-- Add Expense Form (35%) -->
        </Grid.ColumnDefinitions>

        <!-- Expenses Ledger -->
        <Grid Grid.Column="0" Margin="0,0,16,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Total Banner -->
            <Border Grid.Row="0" Style="{StaticResource CardPanel}" Margin="0,0,0,12" Padding="14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="سجل المصاريف والنفقات التشغيلية" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="إجمالي المصاريف:" FontWeight="SemiBold" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding TotalExpenses, StringFormat='{}{0:N2} دج'}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DangerBrush}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Table -->
            <Border Grid.Row="1" Style="{StaticResource CardPanel}" Padding="0">
                <DataGrid ItemsSource="{Binding Expenses}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="رقم السند" Binding="{Binding ExpenseNumber}" Width="150"/>
                        <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="140"/>
                        <DataGridTextColumn Header="فئة المصروف" Binding="{Binding CategoryName}" Width="130"/>
                        <DataGridTextColumn Header="المبلغ" Binding="{Binding Amount, StringFormat='{}{0:N2} دج'}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="البيان" Binding="{Binding Title}" Width="180"/>
                        <DataGridTextColumn Header="ملاحظات" Binding="{Binding Notes}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Add Expense Form -->
        <Border Grid.Column="1" Style="{StaticResource CardPanel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="تسجيل مصروف جديد" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>

                    <TextBlock Text="فئة المصروف:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" DisplayMemberPath="Name" Height="40" Margin="0,0,0,14"/>

                    <TextBlock Text="المبلغ (دج):" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" Height="40" FontSize="16" FontWeight="Bold" Margin="0,0,14,0"/>

                    <TextBlock Text="البيان / المستفيد:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Height="38" Margin="0,0,14,0"/>

                    <TextBlock Text="ملاحظات:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,20,0"/>

                    <Button Content="💸 تسجيل المصروف وخصمه من الصندوق" Command="{Binding SaveExpenseCommand}" Style="{StaticResource DangerButton}" Height="48" FontSize="15"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
