<UserControl x:Class="SalesManagement.Desktop.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Store Information -->
        <Border Grid.Column="0" Style="{StaticResource CardPanel}" Margin="0,0,16,0">
            <StackPanel>
                <TextBlock Text="🏢 بيانات المحل / المؤسسة (ترويسة الفاتورة)" FontSize="16" FontWeight="Bold" Margin="0,0,0,16"/>

                <TextBlock Text="اسم المحل / الشركة:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBox Text="{Binding StoreName, UpdateSourceTrigger=PropertyChanged}" Height="38" Margin="0,0,0,12"/>

                <TextBlock Text="رقم الهاتف:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBox Text="{Binding StorePhone, UpdateSourceTrigger=PropertyChanged}" Height="38" Margin="0,0,0,12"/>

                <TextBlock Text="العنوان التجاري:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBox Text="{Binding StoreAddress, UpdateSourceTrigger=PropertyChanged}" Height="38" Margin="0,0,0,12"/>

                <TextBlock Text="الرقم التعريفي الجبائي (NIF / NIS):" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBox Text="{Binding TaxNumber, UpdateSourceTrigger=PropertyChanged}" Height="38" Margin="0,0,20,0"/>

                <Button Content="💾 حفظ الإعدادات" Command="{Binding SaveSettingsCommand}" Style="{StaticResource SuccessButton}" Height="45"/>
            </StackPanel>
        </Border>

        <!-- Backup & Restore & Database Connection -->
        <Border Grid.Column="1" Style="{StaticResource CardPanel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Connection Configuration -->
                    <TextBlock Text="🔌 إعدادات خادم قاعدة البيانات (MySQL)" FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,6,0">
                            <TextBlock Text="عنوان الخادم (Host / IP):" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding DbHost, UpdateSourceTrigger=PropertyChanged}" Height="38"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="6,0,0,0">
                            <TextBlock Text="المنفذ (Port):" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding DbPort, UpdateSourceTrigger=PropertyChanged}" Height="38"/>
                        </StackPanel>
                    </Grid>
                    <Button Content="⚡ اختبار الاتصال بالسيرفر" Command="{Binding TestConnectionCommand}" Style="{StaticResource PrimaryButton}" Height="38" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding ConnectionStatus}" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,16"/>

                    <Separator Margin="0,0,0,16"/>

                    <!-- Backup -->
                    <TextBlock Text="💾 النسخ الاحتياطي لقاعدة بيانات MySQL" FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>
                    <TextBlock Text="يتم أخذ نسخة كاملة من جداول النظام والبيانات والمعاملات باستخدام أداة mysqldump المحلية لحماية بياناتك من الضياع." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,16"/>

                    <TextBlock Text="مجلد حفظ النسخ الاحتياطية:" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding BackupFolder, UpdateSourceTrigger=PropertyChanged}" Height="38" Margin="0,0,16,0"/>

                    <Button Content="🚀 أخذ نسخة احتياطية فورية الآن" Command="{Binding CreateBackupCommand}" Style="{StaticResource SuccessButton}" Height="48" FontSize="15" Margin="0,0,0,16"/>

                    <!-- Status output -->
                    <Border Background="#F8FAFC" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="12" MinHeight="80">
                        <TextBlock Text="{Binding BackupStatus}" TextWrapping="Wrap" FontSize="12" Foreground="{StaticResource PrimaryBrush}"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
